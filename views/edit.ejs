<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your message</title>
</head>
<body>
    <h3>Edit your chat :<%= chat._id %></h3>
    <p>This chat was sent from:<b><%= chat.from  %></b>  - > <b><%= chat.to %></b></p>
    <form method="POST" action="/chats/<%=chat._id %>?_method=PUT">
        <textarea name="message" placeholder="<%= chat.message %>"></textarea>
        <br>
        <button>Update the message</button>
    </form>
</body>
</html>